# planos Phase 1 — Ralph progress log

## Iteration 1 — 2026-05-16
- Reset greenfield repo, deep-interview spec, consensus plan all complete (pending-approval → user approved via /ralph).
- PRD written: 26 stories, milestone-locked order M0 → M2-thin → M1(HARD GATE) → M2-full → M3 → M4 → M5.
- Source plan: .omc/plans/planos-phase1-consensus.md ; spec: .omc/specs/deep-interview-planos-phase1.md ; design: docs/design.md
- Tech constraints: Node 20+, node:http only, zero runtime deps in blocking path, React 19 + Vite + vite-plugin-singlefile, hand-rolled validator, committed plugin/dist/index.html.

### Codebase patterns / learnings
- Repo is greenfield: only README.md + docs/design.md + .omc/. Target structure = design.md §8.
- AC-17 invariant: NO model call inside blocking `bin/planos exit` path (IDs are agent-authored by design — that's why §6 mechanisms exist).
- Milestone 1 is a HARD GATE: frozen bars (ID-preservation ≥95%, convergence ≤2 iter ≥90%), no tuning, explicit failure/escalation branch.

### Status — Milestone 0 (code) COMPLETE & verified
- US-001 ✅ packaging skeleton (marketplace.json, plugin.json, hooks.json 96h, bin/planos dispatcher) — JSON valid, subcommands dispatch, evidence captured.
- US-002 ✅ node:http server skeleton — 20/20 smoke tests pass incl. real child-process flush-then-exit-0 ordering proof + EADDRINUSE retry. Zero deps.
- US-003 ✅ React 19 + Vite single-file SPA shell — one self-contained plugin/dist/index.html (197 KB), offline, npx tsc --noEmit = 0 errors after adding @types/react + tsconfig.
- US-004 ✅ schema + zero-dep hand-rolled validator + deterministic prose fallback — 16/16 tests, field-level deny-loop error strings, validator-choice ADR (docs/adr/0000), clean diagnostics.
- US-005 ✅ eval harness scaffold — 7/7 tests, 3 separate AC-19 metric groups wired, AC-12 mechanical |preserved∩expected|/|expected| denominator, offline canned mode, fixture format w/ frozen expected set + decoy slots.
- US-006 ⛔ BLOCKED — plannotator coexistence spike requires a live multi-plugin Claude Code session to observe ExitPlanMode dispatch. Environment-dependent; needs the user to run it (cannot be done autonomously).

### Cleanup applied
- Added @types/react, @types/react-dom, typescript devDeps + tsconfig.json (resolved all SPA TS diagnostics; npx tsc --noEmit clean).
- Removed unused rejectDecision in src/server/index.mjs.

### Genuine blockers requiring user input / environment (ralph escalation)
1. US-006 coexistence spike — needs a live `claude` session with two ExitPlanMode plugins.
2. Milestone 1 HARD GATE — requires ≥5 LIVE Claude Code agent runs through the real thin loop to measure ID-preservation. Not autonomously executable in this session (needs real agent invocations + the user's environment).
3. US-026 live-session smoke — needs a real Claude Code install/run.
- These are fundamental blockers per ralph Escalation_And_Stop_Conditions ("Stop and report when a fundamental blocker requires user input"). Milestones 2-thin/2-full/3/4 code can proceed, but the Phase-1 EXIT GATE cannot be cleared without #2.

## Iteration 2 — 2026-05-16 (user chose: keep building autonomous code through M4)
- US-007 ✅ minimal EnterPlanMode injection — 19/19, 39ms (<<5s AC-1), real per-strategy text flows.
- US-009 ✅ dual ID strategies behind PLANOS_ID_STRATEGY flag — 20/20; NO winner chosen (M1 decision deferred to user live gate); src/schema/index.mjs barrel + activeIdStrategy(); schema 16/16 no regression.
- US-008 ✅ minimal ExitPlanMode round-trip (thin loop) — 13/13; AC-2/3/4; (id,kind,title) echo table present on revise (design.md §6 mech #2); zero egress (socket spy); full canned enter→author→exit→forced-revise→exit cycle; all prior suites no regression.
- Integration fix: enter.mjs now uses real src/schema/index.mjs activeIdStrategy().authoringInstruction (was looking for a wrong export name + dead var); verified opaque-strategy text flows.
- M2-thin COMPLETE. Diagnostics cleaned (removed unused vars/imports in test files).
- Next: M2-full (US-012 harden enter [≈done via US-007], US-013 production parse+fallback AC-2/7, US-014 full round-trip+SPA AC-3/4/5, US-015 deny-envelope+baseRevision race guard AC-5/9/10) — all built PARAMETRIZED over the strategy flag (M1 decision stays deferred).

## Iteration 3+ — ALL autonomous code COMPLETE (18/26 stories pass)
- US-012 ✅ (covered by US-007 parametrized enter, AC-1 39ms; chosen-scheme deferred to user M1 gate)
- US-013 ✅ production ExitPlanMode parse+fallback — 38/38; AC-2/AC-7 hard 100% pass/fail across all malformed classes
- US-015 ✅ deny-envelope serialization + baseRevision race guard — 31/31; AC-5/9/10
- US-016 ✅ React block editor all 7 v1 kinds — 5/5 build test, tsc clean, AC-8
- US-017 ✅ FeedbackEnvelope emission from SPA — 12/12; AC-9 validates vs canonical schema
- US-018 ✅ structural diff engine (LCS-relative-order moves) — 12/12; AC-14
- US-019 ✅ re-anchoring fallback + decoy guard — 11/11; false-attach rate 0; AC-13
- US-014 ✅ full blocking round-trip — FOUND+FIXED real prod bug (bare-envelope POST silently dropped ops+race-guard); 6/6; AC-3/4/5
- US-020 ✅ /planos-plan self-contained Socratic command — 25/25; AC-15/AC-17 no-external-skill
- US-022 ✅ graceful interruption fallback — 17/17; AC-16 degradation
- US-021 ✅ AC-16 canned handoff + AC-17 enforcement — 9/9 + 10/10; import-graph walker VERDICT CLEAN; runtime no-egress/no-spawn; allow/deny boundary doc
- FINAL: 15 suites / 244 tests / 0 fail; npx tsc --noEmit 0 errors; one offline single-file plugin/dist/index.html; AC-17 walker CLEAN. Diagnostics clean (one TS false-positive on `require` in ac17-invariant.test.mjs — genuinely used at runtime L122/285-289).

## TERMINAL CHECKPOINT — remaining 8 stories are FUNDAMENTALLY BLOCKED on the user's environment / live agent runs
- US-006 plannotator coexistence spike — needs a live multi-plugin `claude` session to observe ExitPlanMode dispatch
- US-010 forced-revise MEASUREMENT — ≥30 canned fixtures are autonomously buildable but the story is gated on ≥5 LIVE Claude Code agent runs through the real thin loop
- US-011 Milestone 1 HARD GATE decision — needs the live ID-preservation numbers (frozen bars ≥95% / ≤2-iter ≥90%); explicit failure/escalation branch if neither scheme clears
- US-023 full harness run — ≥5 live agent runs
- US-024 verify FROZEN exit gate — needs live numbers
- US-025 M5 handoff+degradation+offline gate — canned/offline parts proven (US-021/US-013); full gate needs live
- US-026 live-session smoke — real Claude Code install/run (AC-20)
- These require real model invocations + the user's Claude Code environment; they are the deliberate Phase-1 de-risking the consensus plan put in the USER's hands. Not autonomously completable. Per ralph Escalation_And_Stop_Conditions → terminal checkpoint, NOT /cancel (task genuinely incomplete; resumable via PRD + this log).

### Handoff — exactly what the user must run
1. `claude --plugin-dir ./apps/hook` equivalent here: `claude --plugin-dir ./plugin` — AC-20 live smoke + US-006 (install a 2nd ExitPlanMode plugin alongside, observe dispatch).
2. Milestone 1 spike: with the thin loop built, run ≥30 canned + ≥5 LIVE forced-revise cycles per ID strategy (PLANOS_ID_STRATEGY=semantic-slug vs opaque), feed the harness (tests/harness/runner.mjs liveAgentDriver — currently an honest unwired stub; wiring it to a real `claude` invocation is the one remaining integration the user's env enables), record docs/adr/0001-block-id-scheme.md. If neither clears the frozen bars → Phase 1 FAILS, escalate (do NOT tune bars).

## Iteration 4 — 2026-05-16 (RESUMED in a live-claude environment via /autopilot)
- Environment now HAS the `claude` CLI (v2.1.143, node v24) → the 8 live-gated stories are executable. Baseline re-verified: full suite green, `npx tsc --noEmit` exit 0, AC-17 import-graph VERDICT CLEAN.
- US-010 wiring DONE: `tests/harness/live-driver.mjs` (real EnterPlanMode ctx → real `claude -p` author → REAL src/hook/exit.mjs forced-revise w/ (id,kind,title) echo table → `claude -p --resume` revise → measure), `runner.mjs` mode:"live" populates group (iii) via injectable runLiveFixture, `tests/harness/run-live.mjs` billed out-of-band runner, `tests/live-driver.test.mjs` offline wiring proof (falsifier + degrade paths), result-shape.test.mjs scaffold-stub test superseded to the wired contract. `claude -p` + `--resume` session continuity verified.
- 31 new forced-revise fixtures authored (feat-/refac-/invest-, 11+10+10) + example.fixture.json fixed (added meta.createdAt) → 32 total. fixture-format.md contract validated for all 32.
- Live ID-preservation denominator (AC-12 spirit): frozen at AUTHOR time = ids the agent minted in its own author doc (the fixture's canned expectedPreservedIds cannot apply live — the live agent mints its own ids); preserved = authorIds ∩ revisedIds; pure set-intersection, zero runtime judgment; forced-revise feedback demands a structure/ID-preserving revision so "all author ids survive" is well-defined.
- Offline gate groups (i)+(ii) PASS over 32 fixtures: (i) gracefulDegradation+offline true, egress 0; (ii) n=32, idPreservation 1.0 (≥0.95), convergence 1.0 (≥0.90), gateReady. firstTry 1.0 reported/not-gated. Full suite 28/28 pass after wiring; tsc 0; AC-17 CLEAN.
- US-006 CC-dispatch research (documented): multi-plugin same-event hooks ALL fire in parallel (no first-wins/priority); bug #29724 dedups only byte-identical pre-expansion command templates (planos `bin/planos` ≠ plannotator → both fire); PermissionRequest reconciliation = deny-wins conjunction; PermissionRequest does NOT fire under `claude -p` (headless ExitPlanMode probe impossible — corroborate all-fire via UserPromptSubmit). Two stub plugins built (tests/spike/plugin-a bin/stuba, plugin-b bin/stub — distinct templates).
- Live group (iii) COMPLETE: 6 fixtures × {semantic-slug, opaque} = 12 live forced-revise cycles through the real thin loop with a real `claude` agent. EVERY run: rate=1.000, valid, converged, firstTry=1.0; denominators 16–35 (real agent-authored docs); zero regressions. Raw: .omc/research/live-{semantic-slug,opaque}.json.
- US-011 HARD GATE: both schemes cleared every FROZEN bar at the maximum (gate did not discriminate). Tie broken on residual-risk asymmetry → production default = `opaque` (rename-stable; its only theoretical weakness fully neutralised by the always-on §6.2 deny-echo table and empirically proven). semantic-slug retained as validated equal-merit alternative via PLANOS_ID_STRATEGY. Wired: src/schema/id-strategy.mjs PRODUCTION_DEFAULT_STRATEGY="opaque" (DEV_DEFAULT_STRATEGY back-compat alias); activeIdStrategy({})→opaque. ADR: docs/adr/0001-block-id-scheme.md. FROZEN_BARS untouched (asserted by tests/harness/verify-exit-gate.mjs). Failure branch NOT taken (both passed).
- US-006 RESOLVED (descoped per explicit user decision: don't care about plannotator coexistence; refuse-on-collision is acceptable). docs/notes/plannotator-coexistence-spike.md documents CC dispatch semantics (all matching plugins fire in parallel; PermissionRequest=deny-wins; bug #29724 dedups only byte-identical pre-expansion templates — planos≠plannotator so both fire; PermissionRequest absent under `claude -p`) + the decided detect-and-refuse posture feeding Step 2-thin.2/2f.3. Stub plugins tests/spike/plugin-{a,b}. No claude spent on corroboration (not load-bearing for refuse). Saved to project memory.
- US-023/US-024 (AC-18/AC-19): tests/harness/verify-exit-gate.mjs → PHASE 1 EXIT GATE: PASS. Three groups SEPARATE, no single number: (i) deterministic PASS; (ii) canned n=32 id=1.0 conv=1.0 PASS; (iii) live[opaque] runs=6 id=1.0 noRegression PASS (alt semantic-slug also PASS). first-try reported (canned=1, live=1), NOT gated. Bars asserted untampered. .omc/research/phase1-exit-gate.json.
- US-025 (AC-16/AC-7/AC-19i): handoff.test.mjs + exit-production.test.mjs green; canned harness egress=0 + group(i) offline=true.
- US-026 (AC-20): static — marketplace/plugin/hooks.json valid (96h timeout), 213KB offline single-file SPA (no external src), bin enter/exit dispatch OK. live — plugin loads cleanly in real `claude --plugin-dir ./plugin` (result READY, is_error=false). + one full author→forced-revise→revise→APPROVE→allow cycle through the real plugin modules (see below).
- Regression after all wiring: full offline suite 28/28 pass, 0 fail; npx tsc --noEmit exit 0; AC-17 import-graph VERDICT CLEAN. New: tests/harness/live-driver.mjs, run-live.mjs, verify-exit-gate.mjs, tests/live-driver.test.mjs; result-shape scaffold-stub test superseded to wired contract; 31 new fixtures + example.fixture.json createdAt fix (32 total).
- OUTCOME: Phase 1 make-or-break risk (design.md §6) FALSIFIED-CLEAR. All 26 stories pass. Phase 1 exit gate PASS. No escalation/failure branch taken.

# planos Phase 2 — PRD Mode + v2 Vocab + Persisted Revision History — COMPLETE

## Phase 2 — 2026-05-16 (plan: .omc/plans/planos-phase2-plan.md; design.md §9)
- Scope delivered: /planos-prd command (command → blocking CLI, NOT ExitPlanMode), full v2 block vocab (phase, tradeoff, fileChange, code, table, diagram), append-only persisted multi-revision history (prds/<doc-id>/rNNN.json + latest.json, committed), multi-revision SPA history browser, AC-17 RE-ASSERTED for the new bin/planos prd entrypoint.
- Reused VERBATIM from Phase 1 (zero behaviour change): readStdin/extractPlan (extracted to src/hook/roundtrip.mjs), planToDocument/buildDecision/buildReviseMessage/renderEchoTable/toPermissionRequestOutput/startServer, structural diff + reanchor engines, FeedbackEnvelope + baseRevision race guard, opaque ID scheme + §6 mechanisms. exit-*.test.mjs are the regression guard and stay green.

### Milestones P0–P5 (dependency order, all gates met)
- P0 v2 schema engine (HARD GATE) ✅ — validate.mjs V2_KINDS + 6 KIND_VALIDATORS + type:"prd" gating (D5-i reject v2 in plan docs, D5-ii table row/col HARD error); types.d.ts/editor types; structural.mjs TEXT_FIELDS + reanchor v2 map. Gate: AC-P1/P2/P3/P4.
- P1 PRD persistence layer ✅ — src/prd/store.mjs (D1 Option A: rNNN.json + latest.json, committed, append-only, path-traversal-safe, canonical JSON, node:fs-only, no clock). Gate: AC-P9, AC-P10(diff half).
- P2 PRD round-trip handler ✅ — src/hook/prd.mjs handlePrd() boots server DIRECTLY (no ExitPlanMode); buildPrdApiHandlers full chain; prd case added to plugin/bin/planos switch (same resolve(__dirname,'<lit>') pattern as exit). Gate: AC-P7/P8/P10(rt half)/P12.
- P3 /planos-prd command ✅ — plugin/commands/planos-prd.md (two-phase Socratic → v2 author → stdin pipe to bin/planos prd, D4); docs/notes/planos-prd-command.md AC-17 boundary note. Gate: AC-P5/P6 ([M] scripted-smoke documented).
- P4 SPA v2 renderers + history browser ✅ — 6 *View components + dispatcher (exhaustiveness guard); src/editor/history.tsx + App.tsx wiring (D2 minimal: list + pick-view + pick-diff-base, reuses structural.mjs); D3 mermaid bundled at SPA build (offline, SPA-side ONLY, NOT in blocking path); plugin/dist/index.html rebuilt + drift-checked. Gate: AC-P11/P13/P14/P17.
- P5 AC-17 re-assertion + Phase 2 exit gate ✅ (THIS milestone) — see below.

### Milestone P5 deliverables (this iteration)
- tests/harness/import-graph.mjs: ac17Roots() now lists src/hook/prd.mjs + src/hook/roundtrip.mjs + src/prd/store.mjs EXPLICITLY. bin/planos already reaches prd.mjs via the SAME provable resolve(__dirname,'<lit>') unwrap it uses for exit.mjs (the prd case), so the real graph walk already followed that edge; the explicit roots make the AC-17 re-assertion dispatcher-independent. node:fs writes in store.mjs are in-scope-allowed (fs ≠ network/model — same boundary as the exit.mjs browser-opener note). Walk stays VERDICT CLEAN.
- tests/ac17-invariant.test.mjs: extended — (1) static module-set assertion now also requires src/hook/prd.mjs + src/hook/roundtrip.mjs + src/prd/store.mjs in the transitive closure; (2) NEW LAYER 2b runtime test mirrors LAYER 2 exactly but drives handlePrd (scripted seam, tmpdir root) under the same lowest-boundary interceptors (node:net connect / node:dns / node:child_process / fetch / http(s).request) — asserts ZERO non-loopback egress, ZERO agent/process spawn, ZERO agent-SDK, and that store.mjs persisted r001 via node:fs. All existing exit-path assertions intact. 11 passed, 0 failed.
- docs/adr/0002-prd-persistence.md: records D1 (Option A committed, append-only, canonical, path-safe; rationale + consequences), D3 (bundle mermaid, offline, SPA-only, AC-17 stays CLEAN), D5 (i reject v2 in plan / ii table row-col HARD / iii phase.taskIds agent-authored), D6 (lighter-but-rigorous gate, no new frozen bar, no ID re-measurement), and the full AC-P18 reasoned waiver (opaque chosen FOR Phase-2 title churn per ADR-0001 #2; §6 falsifier passed 1.000; no new ID surface; round-trip already proven live; semantic-slug's untested risk moot unless reconsidered). Mirrors ADR-0000/0001 structure.
- tests/harness/prd-smoke.mjs: NEW concrete deterministic persistence proof (D6 cheap gate, NOT a frozen gate, spends NO claude). Drives the REAL bin/planos prd round-trip TWICE for one doc id via the scripted seam against a tmpdir → asserts r001 then r002 persisted, monotonic meta.revision, shared id, append-only immutability, and a correct diffDocuments r001→r002 (p-context=modified, ph-build=modified, t-test=added, s-overview/t-impl=unchanged). Exits 0.

### 18 AC-P → test / doc mapping (plan §6)
- AC-P1  [H] validator accepts/rejects every v2 kind → tests/v2-schema.test.mjs (31/0)
- AC-P2  [H] v2 kinds rejected in type:"plan", accepted in type:"prd" (D5-i) → tests/v2-schema.test.mjs
- AC-P3  [H] degradeToProse → 1 prose block + meta.degraded for malformed PRD → tests/v2-schema.test.mjs + tests/prd-roundtrip.test.mjs (degraded-PRD case)
- AC-P4  [H] structural diff classifies v2 blocks (text→word-diff, non-text→structural, reorder→moved) → tests/diff.test.mjs (17/0) + tests/reanchor.test.mjs (13/0)
- AC-P5  [M] /planos-prd Socratic interview → v2 author → boots server (topic + empty arg) → docs/notes/planos-prd-command.md Scenario A/B; tests/planos-prd-command.test.mjs (52/0) static invariants
- AC-P6  [M] graceful interruption → minimal valid v2 PRD, no crash/loop → docs/notes/planos-prd-command.md Scenario C; tests/planos-prd-interrupt.test.mjs (19/0)
- AC-P7  [H] bin/planos prd round-trip (approve persists+success / revise buildReviseMessage / flush-then-exit-0) → tests/prd-roundtrip.test.mjs (8/0)
- AC-P8  [H] baseRevision race guard fires on PRD round-trip → tests/prd-roundtrip.test.mjs
- AC-P9  [H] saveRevision layout + load/list round-trip + append-only + path-traversal → tests/prd-store.test.mjs (20/0)
- AC-P10 [H] two approves → r001+r002 monotonic, shared id, correct diffDocuments → tests/prd-roundtrip.test.mjs + tests/harness/prd-smoke.mjs (concrete real-binary proof, exit 0)
- AC-P11 [M] multi-revision history browser (list + pick-view + pick-diff-base, D2 minimal) → tests/editor-render.test.mjs (9/0) non-visual; src/editor/history.tsx
- AC-P12 [H] /api/prd/versions full chain + /api/prd/version?v=N, read-only no egress → tests/prd-roundtrip.test.mjs (buildPrdApiHandlers unit)
- AC-P13 [M] SPA renders all 6 v2 kinds in BlockShell (comment affordance) → tests/editor-render.test.mjs
- AC-P14 [H/M] diagram render per D3 (bundled mermaid, offline, never crashes SPA) → tests/editor-render.test.mjs + AC-P17 drift check
- AC-P15 [H] AC-17 RE-ASSERTED for bin/planos prd (runtime + static, store.mjs fs in-scope-allowed) → tests/ac17-invariant.test.mjs (11/0) + tests/harness/import-graph.mjs ac17Roots() (VERDICT CLEAN)
- AC-P16 [D] docs/adr/0002-prd-persistence.md records D1 (+ D3/D5/D6) — DONE
- AC-P17 [H] committed-artifact drift: rebuilt plugin/dist/index.html byte-identical → drift check green (P4)
- AC-P18 [D] no-Phase-2-ID-re-measurement reasoned waiver recorded → docs/adr/0002-prd-persistence.md AC-P18 section

### Decisions D1–D6 (all RESOLVED, user sign-off 2026-05-16, recorded in ADR-0002)
- D1 → Option A: prds/<doc-id>/rNNN.json + latest.json, committed to git, append-only, canonical JSON, path-traversal-safe.
- D2 → Minimal history browser: revision list + pick-to-view + pick-diff-base, reuses src/diff/structural.mjs.
- D3 → Bundle mermaid at SPA build time; runtime fully offline; SPA-side ONLY (NOT in bin/planos exit|prd blocking path); AC-17 import-graph stays CLEAN.
- D4 → stdin handoff: /planos-prd pipes authored v2 JSON into bin/planos prd, reuses readStdin/extractPlan.
- D5 → (i) reject v2 kinds in type:"plan"; (ii) table row/col mismatch = HARD validator error; (iii) phase.taskIds agent-authored like v1 task.deps (no blocking-path referential check).
- D6 → Lighter-but-rigorous Phase-2 gate: 18-AC harness/doc/manual set + full offline suite + tsc + AC-17 import-graph CLEAN + deterministic prd-smoke. NO new frozen numeric bar, NO live ID re-measurement (AC-P18 waiver).

### Final verification (Milestone P5, all green, fresh output)
- node tests/harness/import-graph.mjs → VERDICT CLEAN (now incl. src/hook/prd.mjs + src/hook/roundtrip.mjs + src/prd/store.mjs roots)
- node --test tests/ac17-invariant.test.mjs → tests 1, pass 1, fail 0 (own runner: 11 passed, 0 failed incl. new PRD runtime layer + extended static assertion)
- node --test tests/*.test.mjs tests/harness/*.test.mjs → tests 40, pass 40, fail 0 (FULL suite)
- npx tsc --noEmit → exit 0
- node tests/harness/verify-exit-gate.mjs → exit 0 (Phase 1 exit gate NOT regressed; all exit-*.test.mjs green)
- node tests/harness/prd-smoke.mjs → r001+r002 persisted, monotonic, shared id, append-only, diff correct, exit 0
- Per-suite (Phase 2 core): v2-schema 31/0 · diff 17/0 · reanchor 13/0 · prd-store 20/0 · prd-roundtrip 8/0 · planos-prd-command 52/0 · planos-prd-interrupt 19/0 · editor-render 9/0 · ac17-invariant 11/0
- OUTCOME: Phase 2 COMPLETE. All 18 AC-P satisfied (11 [H] harness-asserted, 2 [D] doc artifacts, 4 [M] scripted-smoke documented, 1 [H/M] mixed). AC-17 RE-ASSERTED (not weakened) for the new bin/planos prd entrypoint — runtime + static layers CLEAN with the prd transitive set in the audited closure. Phase 1 NOT regressed. D6 honoured: no new frozen numeric bar, no Milestone-1 ID re-measurement. No escalation/failure branch taken.

# planos Phase 3 — Diff Review Mode + v3 `diff` Block Kind — IN PROGRESS

## Phase 3 — 2026-05-16 (plan: .omc/plans/planos-phase3-plan.md; design.md §9; architect-produced, structure mirrors Phase 2)
- Open Decisions R1–R7 signed off by user 2026-05-16 (recorded in plan "Resolved Decisions"): R1=Option A (gh/git ingestion pre-server CLI; src/review/ingest.mjs PURE text→blocks parser, zero node:child_process; blocking path stays model/net/spawn-free); R2=Option A (ephemeral — NO review persistence, store work units DROPPED); R3=both sources; R4=stdin handoff; R5=hunk-level verdict in BlockComment, NO new envelope op; R6=binary/rename empty-hunks stubs + size-cap elision; R7=v1∪v3 allowed-kinds in type:"diff-review".
- Reused from Phase 1/2 (zero behaviour change): hand-rolled validator engine, command→blocking-CLI handler shape (prd.mjs template), readStdin/extractPlan, buildDecision/buildReviseMessage/renderEchoTable/startServer, structural diff + reanchor, FeedbackEnvelope + baseRevision race guard, opaque IDs + §6 mechanisms, single-file SPA build, AC-17 two-layer enforcement.

### Milestone R0 — v3 schema engine (HARD GATE) ✅ COMPLETE & independently verified
- R0.1 src/schema/validate.mjs: V3_KINDS=["diff"]; V3_KIND_SET; DIFF_REVIEW_KIND_LIST (v1∪v3 per R7); DIFF_STATUS/DIFF_LINE_OPS/BLOCK_COMMENT_VERDICTS enums; requireInteger pusher; KIND_VALIDATORS.diff + validateHunk/validateDiffLine/validateBlockComment (exact §3.1 shapes — empty hunks/comments allowed; text via isString; hunkId nonEmpty; BlockComment.hunkId string|null; verdict/status/op via checkEnum); validateBlock third isDiffReview tier (diff rejected outside type:"diff-review"; v2 PRD kinds rejected inside; invalid-kind msg reflects active tier).
- R0.2 types: DiffLine/Hunk/BlockComment/DiffBlock interfaces + Block union 13→14 in src/schema/types.d.ts + src/editor/types.ts; minimal `case 'diff': body=null; // R4: full DiffView` arm in src/editor/blocks.tsx to satisfy _never exhaustiveness guard (tsc=0). plugin/dist/index.html rebuilt (required to keep AC-P17 byte-identical drift test green; +38 B = 3,410,203 B ≈3.25 MB, under 4 MB cap).
- R0.3 src/diff/structural.mjs TEXT_FIELDS += diff:["path"]; src/diff/reanchor.mjs PRIMARY_FIELD += diff:["path"].
- R0.4 tests/v3-schema.test.mjs (31/0, mirrors v2-schema); tests/diff.test.mjs extended (+4, 21/0); tests/reanchor.test.mjs extended (+2, 15/0).
- Gate AC-R1/AC-R2/AC-R3/AC-R4 satisfied. Independent verify (orchestrator, suite run TWICE — exec-noted flake did NOT recur): full suite 41/41/0 ×2; npx tsc --noEmit exit 0; import-graph VERDICT CLEAN; verify-exit-gate exit 0 (Phase 1 NOT regressed); prd-smoke PASS (Phase 2 NOT regressed). No FROZEN_BARS/metrics/Phase-1/2 behaviour touched; envelope.mjs/src/hook/plugin(bin)/src/server/src/review untouched; zero new runtime deps.

### Milestone R1 — diff-ingestion parser (pure text→blocks) ✅ COMPLETE & independently verified
- R1.1 src/review/ingest.mjs (NEW, ~330 lines, ZERO imports — no node: at all, mirrors structural.mjs purity): ingestUnifiedDiff(diffText,opts) — opts.idPrefix(default "dr") + opts.maxLinesPerHunk(default 2000, exported MAX_LINES_PER_HUNK). One diff block/file; status from git extended headers (new file→added, deleted file→deleted, rename from/to→renamed+oldPath, Binary/GIT binary patch→binary, else modified); binary/rename-no-content → empty hunks:[] (R6). @@ -a[,b] +c[,d] @@ section parse, omitted ,count⇒1; per-line op " "/"+"/"-"; "\ No newline" folded (annotates EOL not content); comments always [] at ingestion. Deterministic content-independent ids: block <idPrefix>-<n>, hunkId <blockId>-h<n> (§3.4/ADR-0001 opaque). R6 size cap: overflow elided with one explicit context DiffLine "… N lines elided …", never throws/blocks (readStdin degrade doctrine); malformed/empty→[]. Top-of-file AC-17 purity contract comment (R1 Option A).
- R1.2 store DROPPED (R2=ephemeral).
- R1.3 tests/review-ingest.test.mjs (NEW, 17/0): single/multi-file multi-hunk, added/deleted/renamed(±content)/binary, omitted-count, section heading, no-newline fold, oversized-hunk elision no-throw, custom idPrefix, degrade→[], determinism (deep-equal + byte-identical JSON ×2), every-block-validates in type:"diff-review" doc (AC-R9), static-purity scan (AC-R10 static half, comment-stripped).
- Iteration fix: removed a buggy "bare empty line = context line" heuristic that mis-counted split("\n") trailing-\n artifact; genuine empty-content context arrives as single-space line (text:"") already handled; corrected one fixture's omitted-count header.
- Gate AC-R9 + AC-R10(static half). Independent verify (orchestrator): full suite 42/42/0; npx tsc --noEmit exit 0; import-graph VERDICT CLEAN; verify-exit-gate exit 0; prd-smoke PASS. purity hard-check: only match is doc-comment prose (line 14), executable source pure, import-graph CLEAN confirms. No FROZEN_BARS/schema/hook/server/plugin/diff touched; zero new runtime deps.

### Milestone R2 — diff-review round-trip handler (boots server directly) ✅ COMPLETE & independently verified
- R2.1 src/hook/review.mjs (NEW): handleReview() mirrors handlePrd() verbatim — SCRIPTED vs REAL-SPA seam identical; reuses readStdin/extractPlan (R4 stdin), planToDocument degradeOpts.type="diff-review", startServer/buildDecision/buildReviseMessage/renderEchoTable/toPermissionRequestOutput/buildSpaHtml/openBrowserReal. EPHEMERAL (R2): NO store import, NO saveRevision, NO loadLatest. APPROVE → structured ReviewRoundTrip hookSpecificOutput (per-hunk verdicts+comments+overall, persisted:false) derived from diff blocks' comments[] (R5 BlockComment{verdict}); REVISE → buildReviseMessage (directive + (id,kind,title) echo + canonical JSON) verbatim; flush-then-exit-0. buildReviewApiHandlers modeled on buildPlanApiHandlers (current + optional injectable previous, NOT prd multi-revision chain — no persisted chain; documented in header). AC-17 boundary doctrine in header.
- R2.2 plugin/bin/planos: `review` case via same provable-literal import(resolve(__dirname,'../../src/hook/review.mjs')) as prd; usage string → "enter, exit, prd, review".
- R2.3 tests/review-roundtrip.test.mjs (NEW, 8/0): SCRIPTED decisionProvider + child-process pattern from prd-roundtrip. AC-R7 (approve→structured envelope+NO persistence+exit0; revise→buildReviseMessage; degraded type:"diff-review"); AC-R8 (baseRevision race guard via checkBaseRevision — fresh applies, stale trips re-render); AC-R12 envelope half (editBlock patching comments[] with BlockComment{verdict} through buildDecision looksLikeBareEnvelope unchanged, NO new op R5). NO-persistence asserted: reviewsDirExists()===false on every path + review.persisted===false in JSON.
- Gate AC-R7/AC-R8/AC-R12(envelope half). Independent verify (orchestrator): full suite 43/43/0; npx tsc --noEmit exit 0; import-graph VERDICT CLEAN (review.mjs reached via prd-style provable literal); verify-exit-gate exit 0; prd-smoke PASS; exit-*/prd-* regression guard green; review-roundtrip ✔. Zero behaviour change to exit/prd/roundtrip/server/schema/diff/ingest (review.mjs only imports them); no store.mjs; zero new runtime deps.

### Milestone R3 — /planos-review self-contained command ✅ COMPLETE & independently verified
- R3.1 plugin/commands/planos-review.md (NEW): mirrors planos-prd.md verbatim. Arg-shape detection (PR# / #123 / PR URL → gh pr diff; git range main..HEAD/HEAD~3/sha..sha → git diff; empty → ask — R3 both sources). Phase 1 brief scope interview; Phase 1b explicit pre-server gh/git as agent's OWN Bash tool use (NEVER in bin/planos review); Phase 2 v3 diff-review authoring (recommends shelling to pure src/review/ingest.mjs; direct-authoring alternative). Embeds full v3 diff/Hunk/DiffLine/BlockComment schema (v1∪v3, v2 PRD rejected R7) + worked example w/ binary stub + ID-stability rules; stdin pipe to node ${CLAUDE_PLUGIN_ROOT}/bin/planos review (R4); graceful-interruption + ingestion-failure recovery. Self-contained (no external skill).
- R3.2 docs/notes/planos-review-command.md (NEW): mirrors planos-prd-command.md. Centerpiece = gh/git-vs-AC-17 boundary (R1 Option A, refs plan §4.3: gh=network egress so stays strictly pre-server live-agent; blocking path model/net/spawn-free; ingest.mjs pure zero node:child_process). Scenarios A(PR#)/B(git range)/C1–C4(interruption+ingestion failure) + file-ownership table.
- R3.3 tests/planos-review-command.test.mjs (58/0) + tests/planos-review-interrupt.test.mjs (22/0): mirror planos-prd-* static-invariant style. AC-R5 (PR#/git-range/empty arg handling, stdin pipe, no ExitPlanMode, v3 schema+example embedded); AC-R6 (interruption→minimal valid v3 doc w/ diff+openQuestion→still pipes, no crash/loop). Boundary asserted: "command FORBIDS gh/git inside the blocking bin/planos review path" + "states gh/git runs BEFORE server boots" + "blocking path network/model/spawn-free" + interrupt path holds boundary.
- Gate AC-R5/AC-R6. Independent verify (orchestrator): full suite 45/45/0; npx tsc --noEmit exit 0; import-graph VERDICT CLEAN; verify-exit-gate exit 0; prd-smoke PASS; review cmd suites 58/0 + 22/0. Doc/prompt/test assets ONLY — zero source/runtime change; gh/git kept strictly pre-server (R1 Option A) by the command text + asserted by tests.

### Milestone R4 — SPA v3 diff renderer + per-hunk review affordance ✅ COMPLETE & independently verified
- R4.1 src/editor/blocks.tsx: replaced R0 `case 'diff': body=null` placeholder with real DiffView (status badge + mono path header, oldPath→path on rename, empty-hunks[] binary/rename affordance never crashes, per-hunk unified-diff <pre> styled by DiffLine.op like CodeView zero new deps, per-hunk tri-state accept/reject/comment toggle keyed by hunk.hunkId + comment textarea seeded from pre-existing BlockComments); BlockShell-wrapped (block-level comment affordance intact); hunkReview/onHunkReview optional BlockRendererProps (v1/v2 call sites unchanged). src/editor/types.ts: HunkReview iface + optional EditorState.reviewVerdicts (blockId→hunkId→review) minimal fold-in. src/editor/App.tsx: reviewVerdicts state threaded + folded into EditorState memo (drops neutral empty-text comment verdicts).
- R4.2 src/editor/envelope.impl.mjs: per-hunk verdict → EXISTING editBlock op patch:{comments:[BlockComment{commentId,hunkId,text,verdict}]}; deterministic commentId=<blockId>-c<n> (stable doc-hunk order, sorted fallback); NO new EDIT_OPS discriminant; envelope.ts/.d.mts unchanged (types flow via EditorState/Partial<DiffBlock>). src/schema/envelope.mjs BYTE-UNCHANGED (R5, orchestrator-verified git diff --stat empty).
- R4.3 npm run build:editor → plugin/dist/index.html rebuilt: 3,410,203 → 3,414,771 B (+4,568; no new runtime dep; ~0.74 MB headroom <4 MB cap). Orchestrator independent determinism re-check: cp + rebuild + cmp → BYTE-IDENTICAL.
- R4.4 tests/editor-render.test.mjs +AC-R11 (10/0): diff discriminant, binary stub, per-hunk affordance, BlockShell intact, _never satisfied by real render + AC-P17/AC-R15 drift PASS. tests/envelope-emit.test.mjs +AC-R12 SPA-emit (14/0): per-hunk verdict→editBlock comments[] patch in exact shape review.mjs deriveReviewResult reads, only existing op discriminants, validateEnvelope round-trip, byte-stable re-emission, neutral-empty emits nothing.
- Gate AC-R11/AC-R12(SPA-emit half)/AC-R15. Independent verify (orchestrator): full suite 45/45/0; npx tsc --noEmit exit 0; import-graph VERDICT CLEAN; verify-exit-gate exit 0; prd-smoke PASS; editor-render 10/0 + envelope-emit 14/0; independent drift cmp BYTE-IDENTICAL; dist <4 MB; envelope.mjs byte-unchanged. ZERO new runtime deps; no schema/hook/server/diff/review/bin/commands/FROZEN_BARS touched. AC-R12 envelope-round-trip half remains green via R2 review-roundtrip.
